# Tiered PyProject.toml Template for CI Framework
# Based on the proven 4-tier quality system from CI Framework

[build-system]
requires = ["{{ build_backend | default('hatchling') }}"]
build-backend = "{{ build_backend | default('hatchling') }}.build"

[project]
name = "{{ project_name }}"
version = "{{ version }}"
description = "{{ description }}"
authors = [
    { name = "{{ author_name }}", email = "{{ author_email }}" }
]
dependencies = [
    # Keep project dependencies minimal - use pixi features for dev dependencies
]

[project.optional-dependencies]
# Optional dependencies for different use cases

[tool.pixi.project]
name = "{{ project_name }}"
version = "{{ version }}"
channels = ["conda-forge"]
platforms = ["{{ platforms | default('linux-64, osx-64, win-64') }}"]

# Core runtime dependencies
[tool.pixi.dependencies]
python = "{{ python_version | default('3.12.*') }}"
# Add core runtime dependencies here

# ===== TIERED QUALITY FEATURES =====
# TIER 1: Essential Quality Gates (ZERO-TOLERANCE)
[tool.pixi.feature.quality.dependencies]
# Core Testing Framework
pytest = ">=8.0.0"
pytest-cov = ">=4.0.0"
pytest-timeout = ">=2.1.0"
pytest-asyncio = ">=0.21.0"
pytest-xdist = ">=3.3.0"

# Type Checking
mypy = ">=1.0.0"

# Core Linting & Formatting
ruff = "*"

# TIER 2: Extended Quality & Security
[tool.pixi.feature.quality-extended.dependencies]
# Security Scanning
bandit = ">=1.7.0"
pip-audit = ">=2.6.0"

# Code Quality Analysis
radon = ">=6.0.0"
vulture = ">=2.7.0"

# Git Hooks
pre-commit = ">=3.0.0"

# TIER 3: CI/CD & Build
[tool.pixi.feature.quality-ci.dependencies]
# CI Reporting
pytest-json-report = ">=1.5.0"
pytest-html = ">=4.0.0"

# Build Tools
python-build = ">=0.10.0"

# Coverage Tools
coverage = ">=7.0.0"

# TIER 4: Development & Specialized Tools
[tool.pixi.feature.dev-specialized.dependencies]
# Development tools (customize based on project needs)
yamllint = "*"
actionlint = "*"
# Add project-specific dev tools here

[tool.pixi.environments]
# Basic runtime environment
default = {solve-group = "default"}

# Quality gate environments (tiered approach)
quality = {features = ["quality"], solve-group = "default"}
quality-extended = {features = ["quality", "quality-extended"], solve-group = "default"}
quality-full = {features = ["quality", "quality-extended", "quality-ci"], solve-group = "default"}

# Development environment (full quality + specialized tools)
dev = {features = ["quality", "quality-extended", "quality-ci", "dev-specialized"], solve-group = "default"}

# CI environment (quality + CI reporting)
ci = {features = ["quality", "quality-ci"], solve-group = "default"}

[tool.pixi.tasks]
# ===== TIER 1: CORE DEVELOPMENT TASKS (ESSENTIAL) =====
# Installation & Setup
install-editable = "pip install -e ."
dev = "echo 'Development environment ready'"

# Testing (ZERO-TOLERANCE QUALITY GATES)
test = "pixi run -e quality test-impl"
test-impl = "pytest {{ test_path }} -v"
test-cov = "pixi run -e quality test-cov-impl"
test-cov-impl = "pytest {{ test_path }} --cov={{ package_name }} --cov-report=term-missing --cov-report=xml"
test-unit = "pixi run -e quality test-unit-impl"
test-unit-impl = "pytest {{ test_path }}unit/ -v"
test-integration = "pixi run -e quality test-integration-impl"
test-integration-impl = "pytest {{ test_path }}integration/ -v"

# Quality Gates (CRITICAL - MUST PASS)
lint = "pixi run -e quality lint-impl"
lint-impl = "ruff check {{ source_path }} --select=F,E9"
lint-full = "pixi run -e quality lint-full-impl"
lint-full-impl = "ruff check {{ source_path }}"
lint-fix = "pixi run -e quality lint-fix-impl"
lint-fix-impl = "ruff check --fix {{ source_path }}"
format = "pixi run -e quality format-impl"
format-impl = "ruff format {{ source_path }}"
format-check = "pixi run -e quality format-check-impl"
format-check-impl = "ruff format --check {{ source_path }}"
typecheck = "pixi run -e quality typecheck-impl"
typecheck-impl = "mypy {{ source_path }}"

# Combined Quality Check - 🚨 MANDATORY BEFORE COMMIT
# Depends on `lint-full`, not `lint`: every project scaffolded from this
# template inherits this gate, and `lint` (`--select=F,E9`) is strictly weaker
# than the full configured ruff ruleset their CI runs (#271). `lint` stays
# defined as the fast inner-loop check.
quality = { depends-on = ["test", "lint-full", "format-check", "typecheck"] }

# Emergency Quality Fix - USE FOR "Found X errors" CI FAILURES
emergency-fix = "pixi run lint-fix && pixi run format && pixi run test"

# ===== TIER 2: SECURITY & COMPLIANCE TASKS =====
# Security Scanning
security-scan = "pixi run -e quality-extended security-scan-impl"
security-scan-impl = "bandit -r {{ source_path }} --severity-level high -x {{ test_path }}"
safety-check = "pixi run -e quality-extended safety-check-impl"
safety-check-impl = "pip-audit --verbose"
complexity-check = "pixi run -e quality-extended complexity-check-impl"
complexity-check-impl = "radon cc {{ source_path }} --min B"
dead-code-check = "pixi run -e quality-extended dead-code-check-impl"
dead-code-check-impl = "vulture {{ source_path }} --min-confidence 80"

# Static Analysis Suite
static-analysis = { depends-on = ["security-scan", "safety-check", "complexity-check", "dead-code-check"] }

# Comprehensive Check
check-all = { depends-on = ["quality", "static-analysis"] }

# ===== TIER 3: CI/CD & DEPLOYMENT TASKS =====
# CI-specific variants
ci-test = { cmd = "pixi run -e quality ci-test-impl", env = { ENVIRONMENT = "ci" } }
ci-test-impl = "pytest {{ test_path }} --cov={{ package_name }} --cov-report=xml"
ci-lint = "pixi run -e quality ci-lint-impl"
ci-lint-impl = "ruff check {{ source_path }} --output-format=github"
ci-format-check = "pixi run -e quality format-check-impl"

# Build & Deploy
# NOTE: build-impl invokes `python -m build` (module-style invocation of the
# `python-build` package). The generalized delegation guard
# (test_template_task_tool_delegation_convention) tracks direct tool
# invocations by matching tracked feature-dependency names as the literal
# command token; it does not detect module-style `python -m <pkg>`
# invocations, since the executed binary is `python` (a core dependency, not
# a feature-scoped tool) and the literal string `python-build` never appears
# in the command. This delegation is applied manually as a known limitation
# of the guard, not because the guard flagged it.
build = "pixi run -e ci build-impl"
build-impl = "python -m build"
clean = "rm -rf __pycache__ .pytest_cache .coverage htmlcov .ruff_cache dist build"

# Pre-commit Integration - PREVENT CI FAILURES
pre-commit = "pixi run -e quality-extended pre-commit-impl"
pre-commit-impl = "pre-commit run --all-files"
install-pre-commit = "pixi run -e quality-extended install-pre-commit-impl"
install-pre-commit-impl = "pre-commit install --install-hooks"

# ===== TOOL CONFIGURATIONS =====

[tool.ruff]
# Must track the framework floor (#286): generated projects inherit
# framework code that imports tomllib bare, which requires 3.11+.
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by formatter
    "B008",  # do not perform function calls in argument defaults
]

[tool.ruff.lint.per-file-ignores]
"{{ test_path }}**/*" = ["F401", "F811", "F403"]

[tool.mypy]
# Must track the framework floor (#286): generated projects inherit
# framework code that imports tomllib bare, which requires 3.11+.
python_version = "3.11"
check_untyped_defs = false
disallow_any_generics = false
disallow_incomplete_defs = false
disallow_untyped_defs = false
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
explicit_package_bases = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["{{ test_path }}"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--disable-warnings",
    "--timeout=120",
    "--timeout-method=thread",
    "-ra",
]
timeout = 120
timeout_method = "thread"
markers = [
    "unit: Unit tests (fast, <30s timeout)",
    "integration: Integration tests (medium, <120s timeout)",
    "property: Property-based tests",
    "security: Security tests",
    "slow: Slow tests (extended timeout)",
    "asyncio: Async tests",
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

[tool.coverage.run]
source = ["{{ package_name }}"]
branch = true
parallel = false
omit = [
    "{{ test_path }}*",
    "{{ source_path }}__main__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
show_missing = true
skip_covered = false
precision = 2
fail_under = 90

[tool.coverage.html]
directory = "htmlcov"
title = "{{ project_name }} Coverage Report"

[tool.coverage.xml]
output = "coverage.xml"

# Bandit security scanning configuration
[tool.bandit]
exclude_dirs = ["{{ test_path }}"]
skips = ["B101"]  # Allow assert statements in test files
targets = ["{{ source_path | default('src') }}"]
